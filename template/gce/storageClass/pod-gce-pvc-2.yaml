apiVersion: v1
kind: Pod
metadata:
  name: pod-gce-pv-v2
  labels:
    app: pod-gce-pv-v2
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
    volumeMounts:
      - mountPath: /app-volumes/pod-gce-pv-v2-volume
        name: pod-gce-pv-volume
  volumes:
    - name: pod-gce-pv-volume
      persistentVolumeClaim:
        claimName: gce-pvc-v2
